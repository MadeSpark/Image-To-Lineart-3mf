# 全站访客计数器 Worker URL
# 部署 worker/visitor-counter 后，把 Worker 的 URL 填到这里。
# 示例：VITE_VISITOR_COUNTER_URL=https://visitor-counter.your-subdomain.workers.dev
# 留空或注释掉时，前端会回退到本地浏览器计数（仅记录当前浏览器的访问次数）。
VITE_VISITOR_COUNTER_URL=
