
## 光映浮雕一键预设「3:2」（0902）
- BaseplatePreset 类型（baseplate + lightRelief 双补丁），PalettePanel 可选 presets/onApplyPreset props，
  卡片渲染在底板模板按钮上方，当前设置=预设时绿色高亮。
- 预设数据在 Home.tsx LIGHT_RELIEF_PRESETS（仅 light-relief 模式传入）：crop/150×100/边距2/manual/总高2(A0.2/B1.7)。
- ⚠️ vitest 未开 globals：@testing-library 自动 cleanup 不生效，多测试文件必须显式 afterEach(cleanup)，
  否则上一测试的 DOM 残留导致 "Found multiple elements"。

## 预设卡片去描述文本（0902 晚）
- 用户反馈预设标题就是 "3:2" 不需要其他文本 → BaseplatePreset 删 description 字段，卡片只渲染 name。
