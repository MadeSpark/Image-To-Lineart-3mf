# 2026-08-24

## 08-25 凌晨：存量测试修复 + 反向堆叠去顶盖层
- **5 个存量测试全部修复（现 43/43 通过）**：
  - Home.test：恢复默认配置已收进 WorkbenchHeader 设置下拉，测试改为 fireEvent.click('设置') 后断言；版本断言改为 /^v\d/（badge 只显示 v+版本号）
  - generatorStore.test：持久化 key 已改为分模式（-filigree/-seal/-light-relief/-shared 四个 key），测试改为 filigree key 存 lineart/baseplate/extrude、shared key 存 printBed/customThreeMfProfile
  - generator.test 合并 3MF：`build3mfExternalCompositeObject` 补回 `name="${escapeXmlAttribute(name)}"` 属性（参数原本就传了）；model_settings part 断言更新为 id=1/2（底板/线稿子对象）
- **反向堆叠去掉 B 面顶盖层（背景顶层）**（用户需求 + 二次修正：移除的是 B 面侧顶盖，不是 A 面侧底座）：
  - 背景下半（底座 `[0, faceBZMm]`）始终保留——贴热床实心基座，避免悬空
  - 浮雕保持在原位 `[faceBZMm, bFaceTopMm]`，仅翻转顶/底顺序（顶面固定 bFaceTopMm、底面随灰度变化）
  - 背景顶层（顶盖 `[bFaceTopMm, totalHeightMm]`）反向时省略——浮雕顶面固定已形成平面顶
  - `buildLightRelief3mfPackage` 与 `buildLightReliefPreviewModelGltfBlob` 同步
  - 新增测试 `skips the B-face top plate layer when reverse stack is enabled`（通过 glTF accessor min/max[1] 断言各层 Z 范围）

## 图片放置规则（底板规则）四模式上线
- `ImagePlacement = 'fit' | 'center' | 'stretch' | 'crop'`，字段 `BaseplateSettings.imagePlacement`（types/generator.ts）
- 三套模式默认值均加 `imagePlacement: 'fit'`（generatorStore.ts），旧 localStorage 经 `{...default, ...parsed}` 自动回退
- **图片居中最终语义（用户二次修正）**：保持原比例、不裁剪、不超画布的情况下最大化 = contain 到整个画板（忽略安全边距）；A 面（layoutLineLoops）与 B 面 halftone 均以整个画板为目标（center 时跳过 padding 实心边环），保证 A/B 对齐
- stretch = 非等比拉伸铺满安全区；crop = 中间裁剪等比铺满安全区
- UI：PalettePanel「底板规则」四选一按钮组，仅 outline 之外模板显示

## 光映浮雕 B 面反向堆叠
- `LightReliefSettings.bFaceReverseStack: boolean`（默认 false）
- `buildHalftoneReliefMesh` 增加 reverseStack 参数：顶面固定 zStart+maxHeightMm，底面 = zStart+maxHeightMm-高度（层序 1,2,3→3,2,1）
- UI 开关在 LightReliefPanel（透光浮雕区），开启时琥珀色提示"不建议站着打印"
- 3MF 导出与 gltf 预览两条链路都已传入

## 分层预览加入 B 面（灰色调）
- PreviewCanvas 分层预览分支：lineart 模式 B 面线稿用灰色 #6b7280/0.7；halftone 模式新增图像层（buildBFaceGrayOverlayDataUrl，深灰90~近白235）
- 图层类型扩展 `image?: {href,x,y,width,height}`，渲染器支持 <image>，layers 过滤条件改为 path||image

## 验证结论
- tsc 通过；vitest 37 通过 + 5 失败——**5 个失败为存量问题**（已用 git worktree 基线 94b4935 对比确认：Home 文案"恢复默认配置"缺失、generatorStore 三个持久化测试、combined 3mf name="1-a"），与本次改动无关
- vite build 通过（需先手动 rm -rf dist，回收站包装器会拦截 vite 清空 dist/assets）

## 重大事故与修复：git 仓库损坏
- `git stash` 在本机触发 fatal（pack 文件丢失 + refs 目录消失），导致仓库 "not a git repository"
- 修复路径：备份 .git → 重建 refs/heads/main（写入 94b4935+换行）→ 删除孤立 .idx/multi-pack-index → `git fetch https://github.com/MadeSpark/image-to-lineart-3mf.git main`（SSH 不通，HTTPS 可用）→ fsck 通过
- **教训：本项目勿用 git stash；基线对比用 git worktree + node_modules junction（junction 删除必须先单独移除再删目录）**
- 残留：`.git-backup-corrupt-0824/`（损坏时备份）确认无误后可删；远程 origin 是 git@github.com:MadeSpark/image-to-lineart-3mf.git，最新提交 94b4935 已推送到远程

## 长宽标签颠倒修复（上一轮遗留）
- PrintBedPanel「打印盘长度」→widthMm、「打印盘宽度」→depthMm；PalettePanel「矩形长度」→widthMm、「矩形宽度」→heightMm
