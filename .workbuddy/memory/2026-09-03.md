
## 导出空白修复 + 光映浮雕默认镜像（3ad91d6）
- 导出后预览空白根因：PreviewCanvas processing 并入了 exporting → 导出期间卸载预览内容，
  结束后 ThreeDModelViewer 重挂载；缓存命中时 isBuilding=false，大模型静默重新拉取解码 glTF，
  WebGL 上下文重建失败则永久空白。修复：processing 只传 processing，预览导出期间保持挂载。
- ThreeDModelViewer 补 viewerLoading 提示 + load/error/webglcontextlost 监听
  （必须用原生 addEventListener，React 合成事件对 custom element 不可靠）。
- 光映浮雕镜像：defaultLightReliefLineartSettings(mirror:true)，schema v4→v5 迁移
  （light-relief 快照 mirror===false 视为旧默认删字段回落 true）。
- 复现工具：.debug/headless-test/repro-export-blank.cjs + playwright-core(装在
  ~/.workbuddy/binaries/node/workspace) + channel=系统 Chrome（无需下载浏览器）。
  画板徽标正则是 /线稿轮廓/（innerText 有 QAQ 分隔符，/画板 \d/ 匹配不上）。
  合成测试图会被"按颜色取线稿+去杂点"拒收，要用页面 canvas 画抗锯齿灰度图。

## react-dev-locator 仅 dev（vite.config）
- babel 插件 command==='===serve' 才启用；生产包 trae-inspector-* 属性清零，dist 1.8M→1.6M。

## realImageLineDebug 超时修复
- 300s→900s：本机全管线实测 6~8min，原超时必假失败。重跑 387.9s 通过。
- 技能沉淀：~/.workbuddy/skills/browser-e2e-system-chrome/SKILL.md（Playwright+系统 Chrome 零下载 E2E 全套坑）。

## 访客计数器 CORS 修复
- worker `buildCorsHeaders` 未命中白名单时回显 `list[0]`（写死 localhost:5173）→ 部署域名/file:// 跨域失败。
- 修复：命中才回显请求 origin，否则返回空字符串；`ALLOWED_ORIGINS` 补 `null`（file:// 场景）；清理不可达路由。

## 三模式配置完全独立 + schema v6
- **printBedSettings 从 shared 下沉到各模式**：每个模式的 Settings 接口新增 `printBedSettings`；SharedSettings 仅保留 `customThreeMfProfile`。
- **schema v5→v6 迁移**：老 shared 快照里的 printBedSettings 复制到三个模式 key，迁移后删除 shared 里的该字段。
- store 测试更新：shared 测试改为验证各模式独立存储；新增 v6 迁移测试。

## 印章/光映浮雕镜像关闭警告
- 印章不再强制锁定 mirror disabled：改为允许关闭但弹确认对话框 + 橙色常驻警示文案。
- 光映浮雕同理（默认 mirror:true，用户可关，关时同样警告）。

## 预览画布右键拖动平移
- PreviewCanvas 右键（button 2）直接进入 pan 模式，无需先切「拖动」工具。拖动期间切换 grabbing 光标。

## UI 修复：画布提示 pill 文本溢出
- **根因**：`interactionHint`（30+ 字符）被直接塞进带图标的 pill 徽章，文字溢出。
- **修复**：新增 `shortInteractionHint`（≤12 字符）用于 pill 内联；完整提示移到 `title` tooltip。
- 另两处红框排查：左框 badge 代码为 `X 项` 渲染正确；右框 badge 为模板类型（自动轮廓/矩形/圆形），不存在「复制」。

## 光映浮雕预设新增 2:3
- Home.tsx `LIGHT_RELIEF_PRESETS` 新增 `2:3` 预设：与 `3:2` 反向宽高比（100×150mm），其余参数一致（rectangle/crop/margin 2/manual，Z 轴 total 2、faceA 0.2、faceBZ 0.3、faceB 1.7）。
- 激活判定按全字段比对（含 width/height），2:3 与 3:2 不会误判同一激活态。
- PalettePanel.test.tsx 同步加 2:3 的 render+apply 用例；全量 78 测试通过。
